public class BinaryTree<T> extends TreeNodeNode<T> {
    TreeNode<T> root;
    TreeNode<T> left; 
    TreeNode<T> right;

    public BinaryTree(TreeNode root) {
        this.root = root; 
    }

    public buildTree(TreeNode root) {
        this.root = left; 
    }

    public add(Object val) {
        toAdd = new TreeNode(val);
    }
}